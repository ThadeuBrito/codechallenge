<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :origin_station_id %><br>
    <%= f.text_field :origin_station_id %>
  </div>
  <div class="field">
    <%= f.label :destination_station_id %><br>
    <%= f.text_field :destination_station_id %>
  </div>
  <div class="field">
    <%= f.label :line_id %><br>
    <%= f.text_field :line_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
